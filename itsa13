#include <stdio.h>
#include <stdlib.h>

int main(int argc, char *argv[]) {
    int n;
    scanf("%d", &n);
    int max = 0;
    int x;
    for (int i = 0; i < n; i++) {
        scanf("%d", &x);
        if (x > max) {
            max = x;
        }
    }
    printf("%d\n", max);



	return 0;
}
