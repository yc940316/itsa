#include <stdio.h>
#include <stdlib.h>

int main(int argc, char *argv[]) {

    int n;
    int x, y;
    scanf ("%d", &n);
    for (int i = 0; i < n; i++){
        scanf("%d %d", &x, &y);
        int sum = x + y;
        printf("%d\n", sum * sum);
    }




	return 0;
}
